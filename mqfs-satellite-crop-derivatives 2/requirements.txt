# =====================================================================
# MQFS Satellite Crop Derivatives — Python requirements
# =====================================================================
# Core (required to run the pricing pipeline, tests and figures offline)
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
pyyaml>=6.0

# Testing
pytest>=7.0

# --- Optional: geospatial ingestion ("The Oracle") -------------------
# Only needed to ingest real Sentinel-2 imagery. The pipeline runs fully
# offline on the synthetic generator without these. They pull in GDAL and
# are best installed via conda (see environment.yml).
# earthengine-api>=0.1.380     # Google Earth Engine server-side path
# rioxarray>=0.15              # local raster path
# xarray>=2024.0
# geopandas>=0.14
# dask[array]>=2024.0
# rasterio>=1.3

# --- Optional: native C++ pybind11 module ----------------------------
# The C and C++ engines are also callable via ctypes with no Python deps.
# pybind11>=2.11               # only for the optional `mqfs_cpp` module
